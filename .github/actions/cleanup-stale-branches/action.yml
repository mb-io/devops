# This file is generated by typescript
name: cleanup-stale-branches
description: A GitHub Action built using TypeScript
inputs:
  allowedBranchesPattern:
    description: Regex pattern to match branches that are allowed to be deleted.
    required: false
    default: ''
  dryRun:
    description: >-
      If true, the action will only simulate the deletion of branches without
      actually deleting them.
    required: false
    default: 'false'
  githubToken:
    description: GitHub token required for authentication.
    required: true
  ignoreBranchesPattern:
    description: Regex pattern to match branches that should be ignored and not deleted.
    required: false
    default: ''
  lastCommitAgeDays:
    description: >-
      The maximum age (in days) of a branch to be considered for deletion.
      Default is 90 days.
    required: false
    default: '90'
outputs:
  deletableBranches:
    description: Comma-separated list of branches that met deletion criteria.

runs:
  using: 'node20'
  main: '../../../dist/actions/cleanup-stale-branches/index.js'